# README

Docker container to launch a VestaCP controlled container

configured with:

Apache + Nginx
Exim + Dovecot + SpamAssassin + ClamAV
vsftpd
named
iptables + fail2ban
softalicious
remi
postgresql

